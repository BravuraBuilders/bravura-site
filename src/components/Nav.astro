<!-- Fixed centered wordmark; the engine fades it out past the journey (.nav--hidden). -->
<header class="nav">
  <a href="/" class="brand" aria-label="Bravura Builders home">
    <span class="wordmark">
      <span class="wordmark-name">Bravura</span>
      <span class="wordmark-sub">Builders</span>
    </span>
  </a>
</header>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 40;
    display: flex;
    justify-content: center;
    padding: 0.75rem 1rem;
    pointer-events: none;
    transition:
      opacity 0.4s ease,
      transform 0.4s ease;
  }
  .nav--hidden {
    opacity: 0;
    transform: translateY(-12px);
  }
  .brand {
    pointer-events: auto;
    display: inline-flex;
  }
  .brand:hover {
    text-decoration: none;
  }
</style>
