<!-- MANIFESTO — the "bravura" definition + the written-scope / single-point-of-contact pitch. -->
<section class="manifesto">
  <div class="wrap manifesto-inner">
    <p class="dict">
      bra·vu·ra <span>/brəˈvyoo-rə/</span> — <em>noun.</em> great technical skill and brilliance
      shown in a performance.
    </p>
    <p class="manifesto-body">
      We put that skill into the rooms you actually live in. Every Bravura project runs on a
      written scope, a defined schedule, and a single point of contact — so you always know what
      comes next and exactly what it costs.
    </p>
  </div>
</section>

<style>
  .manifesto {
    color: #fff;
    padding: clamp(4rem, 10vw, 7rem) 0;
    background: transparent;
  }
  .manifesto-inner {
    max-width: 760px;
  }
  .dict {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: clamp(1.4rem, 2.6vw, 2rem);
    line-height: 1.4;
    color: #fff;
    margin: 0 0 1.5rem;
  }
  .dict span {
    color: var(--highlight);
    font-style: italic;
    font-size: 0.75em;
  }
  .dict em {
    color: var(--highlight);
    font-style: italic;
  }
  .manifesto-body {
    color: rgba(255, 255, 255, 0.72);
    font-size: clamp(1.05rem, 1.6vw, 1.2rem);
    margin: 0;
  }
</style>
